from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Biuld a ML service that predicts food delivery time',
    author='Muzahid',
    license='',
)
